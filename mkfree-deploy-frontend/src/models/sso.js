import * as usersService from '../services/users';

export default {
  namespace: 'sso',
  state: {
    type:'SIGNIN'
  },
  reducers: {
   
  },
  effects: {
    
  },
  subscriptions: {
    setup({dispatch, history}) {
     
    },
  },
};
